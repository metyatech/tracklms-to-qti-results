#!/bin/sh

set -e

pwsh -NoProfile -File ./verify.ps1

# Update AGENTS.md / CLAUDE.md (non-blocking)
if command -v compose-agentsmd > /dev/null 2>&1; then
  compose-agentsmd || true
  git add AGENTS.md CLAUDE.md || true
fi
